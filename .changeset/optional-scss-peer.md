---
"stylelint-config-standard-vue": minor
---

Declare `stylelint-config-standard-scss` as an optional peer dependency (`>=13.0.0`). It is not installed automatically and is only needed when using the `stylelint-config-standard-vue/scss` config, but package managers can now validate its version when it is installed.
